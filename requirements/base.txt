python-dotenv==1.2.2
aiogram==3.29.0
httpx==0.28.1
asyncpg==0.31.0
alembic==1.14.0
# Alembic dependency only — schema is raw SQL via op.execute(); the app
# runtime uses asyncpg, no ORM/Core models.
sqlalchemy==2.0.49
fastapi==0.138.0
uvicorn==0.49.0
prometheus-client==0.26.0
